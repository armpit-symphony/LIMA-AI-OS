# Consumer Proof Archive Template Fixture

This fixture describes the static evidence packet shape that Sparkbot and Arc Bot repo teams should fill out in their own dry-run proof branches.

It is metadata only. It does not call `LimaKernel`, run `SimulatedDiscoveryAdapter`, import consumer repositories, mutate files, connect to services, or approve production use.
